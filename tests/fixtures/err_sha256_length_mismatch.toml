[package]
name = "sha-mismatch"
version = "1.0"
summary = "Package with mismatched sha256sums"
license = "MIT"
description = "sha256sums has more entries than sources."
sources = ["https://example.org/file1.tar.gz"]
sha256sums = ["abc123", "def456"]

[[package.changelog]]
version = "1.0-1"
date = "2026-06-29"
packager = "Test <test@example.org>"
entries = ["Initial"]
